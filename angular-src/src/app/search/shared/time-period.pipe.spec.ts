import { TimePeriodPipe } from './time-period.pipe';

describe('TimePeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
